# Base URL of the deployed backend (no trailing slash). For local dev:
#   VITE_API_BASE_URL=http://localhost:8000
# In production, set this to your Render backend URL in Vercel's env settings.
VITE_API_BASE_URL=http://localhost:8000

# candidate_id — must be byte-identical to the backend + README + submission form.
VITE_CANDIDATE_ID=manviitnd0408@gmail.com
